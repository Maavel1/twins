# Base URL for the embedded map. The default value is kept in src/data/siteData.js.
VITE_MAP_PROVIDER=yandex

# Later, when you connect a real backend, put public API URLs here.
VITE_API_URL=http://localhost:3000
